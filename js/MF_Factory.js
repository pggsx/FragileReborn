/**
 *
 */
var MF_Factory[];
/**
 *
 */
function fract_store(MixedFraction fract)
{
	MF_Factory.push(fract);
}
/* fract_calc
 * params: MF_Factory[] fract_list
 * calculates Mixed Fractions Objects total sum and returns sum
 */
function MixedFraction fract_calc(MF_Factory[] fract_list)
{
	if(fract_list != undefined)
		{
		MixedFraction sum;
			var x;
			for(x in fract_list)
				{

				}
		}
	else
		{
			alert("Invalid List of Fractions");
		}
	return sum;
}
